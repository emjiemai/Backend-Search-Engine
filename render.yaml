services:
  - type: web
    name: tender-scraper
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: SCRAPER_PORT
        value: 8081
